# PostgreSQL Database Configuration
PGUSER=your_username
PGPASSWORD=your_password_here
PGHOST=localhost
PGPORT=5432
PGDATABASE=cocktail_app
PGSSLMODE=require

# URLs
FRONTEND_URL=http://localhost:8081
REDIRECT_URL=http://localhost:8081/r

# SMTP (dev with MailHog)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_USE_TLS=false
MAIL_FROM="MyCabinet <no-reply@mycabinet.me>"
REPLY_TO=